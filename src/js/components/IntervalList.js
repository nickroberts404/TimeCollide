import React, {Component, PropTypes} from 'react';
import Interval from './Interval.js';

export default class IntervalList extends Component {
	render() {
		return (
			<div className="interval-list"></div>
		)
	}
}